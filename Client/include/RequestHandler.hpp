#ifndef REQUEST_HANDLER_HPP
#define REQUEST_HANDLER_HPP

#include <string>

class RequestHandler
{
public:
    // static std::string formatQueryAvailabilityRequest(int requestID, const std::string &facilityName);
    // static std::string formatBookFacilityRequest(int requestID, const std::string &facilityName, const std::string &userName);
    // static std::string formatChangeBookingRequest(int requestID, const std::string &facilityName, const std::string &userName, const std::string &newUserName);
    // static std::string formatMonitorAvailabilityRequest(int requestID, const std::string &facilityName);
    // TODO: Format idempotent and non-idempotent operation requests
};

#endif // REQUEST_HANDLER_HPP